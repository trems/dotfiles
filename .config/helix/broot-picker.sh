#!/usr/bin/env bash

# HX_PANE=$1
# BR_PANE=$WEZTERM_PANE

# wezterm cli zoom-pane --zoom
 # paths=$(broot --conf ~/.config/broot/hx-open.hjson) # | while read -r; do printf "%q " "$REPLY"; done)
 # selected_file_path=$(broot --sort-by-type-dirs-last -ghi) # | while read -r; do printf "%q " "$REPLY"; done)

# if [[ -n "$selected_file_path" ]]; then
# 	wezterm cli send-text --no-paste --pane-id "$1" ":open ${selected_file_path}"$'\r'
# 	wezterm cli send-text --no-paste --pane-id "$1" $'\x1B' # send Esc key
# 	wezterm cli kill-pane
# fi

